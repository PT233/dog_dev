Vibe coding 工作流

下面是一个非常简单、一定能成功的方法:

* [ ] **1.architecture.md**

**打开ChatGPT 5(不要用别的)，告诉它:**

> 我正在构建一个xxx(详细描述你的产品--功能、目标用户、关键特性，越详细越好)
> 前端使用Next.is，数据库与用户认证使用Supabase.
> 请帮我生成完整的架构设计，包括:
>
> 1.文件与文件夹结构
> 2.每个部分的作用
> 3.状态存储位置
> 4.服务之间如何连接
> 5.请将整个文档用markdown格式输出。

把ChatGPT的输出保存为**architecture.md**放到一个空文件夹中，这个文件夹将作为你的项目根目录。

* [ ] **2.task.md**

跟ChatGPT 5说:

> 基于architecture.md的架构，写一个详细的构建MVP的分步计划。每个任务都应该:
> 1.非常小且可测试
> 2.有明确的开始与结束
> 3.专注于一个问题
> 4.任务要按实现顺序排列
> 5.我会把这些任务交给一个工程LLM，一次只执行一个任务，使我能够测试每一个任务单元

将输出内容保存为task.md，放到同一个项目文件夹中。

* [ ] **3.test.md**

跟chatgpt说：

> 基于task.md的内容，写一个详细的测试计划，测试计划应该：
> 1.全面覆盖单元测试
> 2.功能测试，查看task.md的需求是否被完全满足
> 3.一个简单的系统测试

* [ ] **4.回到Cursor:**

你是一名工程师，正在构建这个代码库。首先在根目录生成一份详细的说明文档，从硬件连线开始，软件环境，烧录，调试，应用等等。硬件连线可以单独出一个html进行连线描述。
你已经拿到了architecture.md、task.md、CLAUDE.md、progress.md仔细阅读这四个文件，确保对要构建的内容没有任何理解偏差，在progress.md文件中查看并更新任务进度。
按task.md中的顺序，一次只完成一个任务
每完成一个任务就进行自动测试。如果测试通过，我会提交到 GitHub，然后你再继续下一个任务

CODING PROTOCOL开发守则:
1.严格用最少的代码完成当前任务
2.不进行大规模改动
3.不做无关编辑，专注于你正在开发的任务
4.代码必须精确、模块化、可测试
5.不破坏现有功能
6.注释简洁明了，使用英文注释
7.如果需要我做任何配置(例如Supabase/AWS)，请明确告诉我

❯ 我已经通过网线连接了树莓派，可以通过ssh连接 ssh ubuntu@192.168.137.100 已经设置了密钥可以免密登录,相机是3D webcam，/dev/video0
  这个相机需要用camera.sh脚本才能开启双目：
  在树莓派的：
  ubuntu@ubuntu:~$ ls
  camera.sh
  脚本需要在推流后启用才能实现双目效果。

查看~/.bashrc 不要让我每次点yes，还有哪些配置一并告知我，配置好后再开始任务




# 调试

你需要阅读 @task.md @progress.md @architecture.md @CLAUDE.md @SETUP_GUIDE.md @QUICK_START.md 以及获取git状态，了解我都做了哪些工作。现在对uart的完整通信回路进行验收测试。


# de_shit
## 总体
审查整个项目代码质量，给我一份"技术债清单"，按严重程度排序。重点关注：

过度设计：用了不必要的抽象、单例、模板、虚函数
职责混乱：一个类/函数干了多件不相关的事
重复代码：相似逻辑在多处出现
死代码：声明了但没人用的函数、变量、文件
命名问题：含义不清、不一致、误导性命名
错误处理缺失：返回值没检查、异常没捕获、HAL 函数没判错
嵌入式特定问题：动态内存、阻塞调用、ISR 里的不当操作
ROS2 特定问题：QoS 没配、回调阻塞、节点生命周期混乱

每条给出：文件:行号、问题描述、修复优先级（高/中/低）、修复成本（小时数估计）




## 对于 STM32 端：
裸机/RTOS 测试比较麻烦，至少做两件事：
关键算法做 host-side 单元测试：把纯逻辑（PID、滤波、协议解析）抽出来在 PC 上用 Unity/CMock 跑测试，不依赖硬件
板上"冒烟脚本"：写个上位机脚本，启动后发一组指令，记录返回数据作为基准。重构后跑一遍，对比输出

## 重构
第三步：小步重构的几种典型模式
按重构类型给你具体的 prompt 模板：

模式 1：拆肥大的文件/函数
一个文件过于臃肿的话请按职责拆成多个文件。要求：
拆完后的每个文件不超过 300 行
给我一份"拆分前后对照表"：原来的函数现在去哪了
不要改任何函数的内部逻辑，只搬位置和调整 include
拆完后头文件接口保持不变（外部调用代码不用改）


模式 2：消除过度抽象
这个项目里有哪些"只有一个实现的接口/抽象基类"？列出来。对每个判断：
这个抽象有没有真实价值（未来真的会有第二个实现吗）
如果没有，给出"内联化"方案：去掉抽象层，直接用具体实现
评估去掉后会影响多少处代码


模式 3：统一命名和风格
整个项目命名风格不一致（有 camelCase 也有 snake_case，有 get_xxx 也有 fetch_xxx）。请：
统计现有命名风格的使用频率
推荐一套统一规范（参考 ROS2 C++ Style Guide 和项目实际情况）

模式 4：消除重复代码
找出项目里 5 处以上的"近似重复代码"（结构相同但变量名/常量不同）。对每组：
提取公共部分，给出建议的抽象方案（函数 / 模板 / 宏，根据情况选）
注意嵌入式约束：不要为了 DRY 引入运行时开销，模板优于虚函数
评估抽象后的可读性变化

模式 5：嵌入式特定问题修复
审查 STM32 端代码，按以下清单逐项检查并报告问题：
 中断服务函数里有没有调用阻塞或非 ISR 安全的 API
 FreeRTOS 任务栈大小是否合理（用 uxTaskGetStackHighWaterMark 评估过吗）
 有没有用 malloc/new（裸机/MCU 上应避免）
 共享变量有没有 volatile 或临界区保护
 串口/SPI/I2C 是否用了 DMA + 中断（vs 阻塞调用）
 HAL_xxx 函数返回值是否检查
 浮点运算是否在 ISR 里（M4F 上要保存 FPU 上下文）