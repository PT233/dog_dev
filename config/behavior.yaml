behavior_node:
  ros__parameters:
    # 输入图像尺寸；像素中心点默认按 320x480 左目图设置。
    image.width: 320
    image.height: 480

    # 视觉目标误差零点。image.center_x/image.center_y 可按相机安装误差做标定微调。
    image.center_x: 160
    image.center_y: 240
