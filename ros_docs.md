请为robot_interfaces节点撰写一份详细的中文代码说明文档,在docs文件夹内创建同名文件夹。

【节点源码/路径】
<在这里粘贴代码或给出文件路径>

【节点功能简述】
<例如：订阅激光雷达数据，进行障碍物检测，发布检测结果>

【输出格式要求】
Markdown 格式，使用二级和三级标题组织内容。

【文档必须包含以下章节】
1. 节点概述：用途、所属功能包、在系统中的角色
2. 依赖项：ROS2 版本、依赖的功能包、第三方库、自定义消息
3. 节点接口清单（用表格列出）：
   - 订阅的话题（名称、消息类型、QoS、回调函数、用途）
   - 发布的话题（名称、消息类型、QoS、发布频率、用途）
   - 提供的服务 / 动作（名称、类型、用途）
   - 客户端调用的服务 / 动作
   - TF 坐标系（监听 / 广播）
4. 参数列表（表格）：参数名、类型、默认值、取值范围、含义、是否动态可调
5. 核心代码逻辑：
   - 类结构和继承关系
   - 构造函数初始化流程
   - 主要回调函数的处理流程（用步骤或流程图描述）
   - 关键算法或状态机说明
6. 生命周期与线程模型：执行器类型（单线程/多线程）、回调组、定时器
7. 启动方式：
   - ros2 run 命令示例
   - launch 文件示例
   - 参数 YAML 配置示例
8. 调试与排错：常见问题、日志查看方法、可视化工具（rqt、rviz）建议
9. 单元测试与集成测试说明（如有）
10. 变更记录与待办事项

【写作风格要求】
- 面向初级 ROS2 开发者，需要解释 ROS2 基础概念
- 代码片段要带行内注释
- 关键流程用 mermaid 流程图表示
- 表格优先于长段落罗列
- 每个回调函数都要说明触发条件和处理结果
            
            

请为以下 STM32 项目撰写一份详细的中文代码说明文档。

【项目源码/路径】
<粘贴代码或给出工程目录路径>

【硬件平台】
- 芯片型号：<如 STM32F407VGT6 / STM32H743VIT6 / STM32G431CBU6>
- 开发板：<如自制板 / 正点原子探索者 / Nucleo-XXX>
- 关键外部器件：<如 W25Q64 Flash、ICM20602 IMU、MX25 SPI Flash 等>

【开发框架】
- HAL / LL / 标准库 / 寄存器裸写：<选一个>
- IDE / 工具链：<如 STM32CubeIDE / Keil MDK / CLion+CubeMX / PlatformIO>
- RTOS：<无 / FreeRTOS / RT-Thread / ThreadX>

【项目用途简述】
<例如：四轴飞行器主控，运行姿态解算、PID 控制、遥控通信>

【输出格式要求】
Markdown 格式，使用二级和三级标题组织。

【文档必须包含以下章节】

1. 项目概述
   - 项目用途、整体架构、主控芯片选型理由
   - 软件分层（BSP / 驱动 / 中间件 / 应用层）

2. 硬件资源分配表（表格）
   - 引脚号、引脚名、复用功能、连接器件、用途、电气特性（上拉/下拉/速度）
   - DMA 通道分配
   - 定时器分配（哪个 TIM 做什么）

3. 时钟树配置
   - HSE/HSI 来源、PLL 倍频分频、SYSCLK/HCLK/PCLK1/PCLK2 频率
   - 各外设时钟来源
   - 建议附一张时钟树文字图或 mermaid 图

4. 外设配置详解（每个用到的外设单独一节）
   每个外设说明：
   - 工作模式（如 SPI 主机全双工、UART 8N1 中断+DMA）
   - 关键寄存器/初始化参数
   - 中断优先级（抢占/子优先级）
   - DMA 配置（方向、循环模式、数据宽度）
   - 引脚映射

5. 中断与优先级表（表格）
   - 中断向量、优先级、回调函数、最大执行耗时估计、是否在 RTOS 临界区

6. 内存布局
   - Flash 分区（Bootloader / App / 参数区 / OTA 备份区）
   - RAM 使用（栈大小、堆大小、静态变量、DMA 缓冲区位置）
   - 链接脚本中的特殊段（如 .ccmram、.dtcm）说明

7. 软件架构与任务划分
   - 主循环 / 状态机 / RTOS 任务列表（任务名、优先级、栈大小、周期、职责）
   - 任务间通信机制（队列、信号量、事件组、消息）
   - 关键全局变量与共享资源保护方式

8. 核心模块代码逻辑
   每个关键模块（如通信协议、控制算法、文件系统）说明：
   - 模块输入输出
   - 调用关系图
   - 关键函数流程（步骤或 mermaid 流程图）
   - 状态机用 stateDiagram 表示

9. 通信协议（如有）
   - 帧格式（用表格列出字节偏移、字段名、长度、含义）
   - 校验方式（CRC8/CRC16/Checksum）
   - 收发流程

10. 配置参数与宏定义
    - 用户可配置的宏（在哪个头文件、含义、默认值、影响范围）
    - 编译选项（Debug/Release 区别）

11. 编译、烧录与调试
    - 编译命令或 IDE 操作步骤
    - 烧录方式（ST-Link / J-Link / DFU / ISP）
    - 调试技巧（SWO/ITM 打印、断点注意事项、watch 变量推荐）
    - 常见编译错误与解决

12. 测试与验证
    - 单元测试方法（如 Ceedling / Unity）
    - 硬件在环测试点
    - 已知问题和待优化项

13. 版本与变更记录

【写作风格要求】
- 面向有 STM32 基础的嵌入式开发者，不解释"什么是 GPIO"这类基础概念
- 代码片段必须带中文行内注释，关键寄存器位要说明
- 涉及时序的地方用时序图描述（mermaid sequenceDiagram 或文字时序）
- 状态机用 mermaid stateDiagram-v2 画
- 表格优先于段落罗列
- 关键参数（波特率、SPI 时钟、PWM 频率）必须给出具体数值和计算依据